# Runtime dependencies for knyc_discord_bot.py (+ score_predictions.py).
# Training/notebook extras (scikit-learn, matplotlib, seaborn, jupyter) are
# NOT needed on the bot host — they only run locally in KNYC_Nowcaster.ipynb.
discord.py>=2.3
python-dotenv>=1.0
requests>=2.28
pandas>=2.0
numpy>=1.24
xgboost>=2.0,<4
joblib>=1.3

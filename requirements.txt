# Runtime dependencies for knyc_discord_bot.py (+ score_predictions.py).
# Training/notebook extras (scikit-learn, seaborn, jupyter) are NOT needed on
# the bot host — they only run locally in KNYC_Nowcaster.ipynb.
discord.py>=2.3
python-dotenv>=1.0
requests>=2.28
pandas>=2.0
numpy>=1.24
xgboost>=2.0,<4
joblib>=1.3
# daily_chart.py renders the #daily-performance image; it pins the Agg backend
# so no display or GUI toolkit is required on the VM.
matplotlib>=3.7
